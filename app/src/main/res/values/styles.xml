<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ImageTheme">

        <item name="android:layout_width">@dimen/imageview_width</item>
        <item name="android:layout_height">@dimen/imageview_height</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:layout_marginStart">@dimen/image_margin_start</item>
        <item name="android:layout_marginTop">@dimen/imageview_margin_top</item>
        <item name="android:layout_marginEnd">@dimen/imageview_margin_end</item>


    </style>

</resources>
